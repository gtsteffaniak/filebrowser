http:
  port: 8080
  baseURL:  "/files/"
server:
  logging:
    - levels: "info|error|warning|debug"
      apiFilter: 'disabled'
  sources:
    - path: "../frontend/tests/playwright-files"
      name: "playwright"
      config:
        defaultEnabled: true
