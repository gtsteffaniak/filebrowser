import { test, expect } from "../test-setup";
import { Page } from "@playwright/test";


// run npx playwright test --ui to run these tests locally in ui mode
test('local testing via local playwright here', async ({
    page,
    checkForErrors,
}) => {

});